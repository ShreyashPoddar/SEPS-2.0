# In production on Vercel, requests to /api are proxied to Render backend via vercel.json.
# If you need to point to a custom API URL, specify it here:
# VITE_API_URL=https://seps-backend.onrender.com/api

# Offline demo mode: keep false so real backend data is used in production.
VITE_USE_MOCK=false

