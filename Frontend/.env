VITE_BACKEND_URL=ececonnect-production.up.railway.app
# Offline demo mode: serve built-in mock data when the backend is unreachable.
# Leave unset/false so real backend failures surface as errors instead of fake data.
# VITE_USE_MOCK=true
