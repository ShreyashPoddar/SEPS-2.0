{
  "name": "seps-monorepo",
  "version": "2.0.0",
  "scripts": {
    "build": "npm --prefix Frontend run build",
    "start": "npm --prefix backend start"
  },
  "dependencies": {
    "@neon/config": "^1.3.0",
    "@neon/env": "^1.2.1"
  },
  "devDependencies": {
    "puppeteer-core": "^25.10.0"
  }
}
